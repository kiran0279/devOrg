trigger LeadTrigger on Lead ( after insert ) {
    // LeadHandler handler = new LeadHandler(); 
    // if (Trigger.isInsert) {
    //     if (Trigger.isAfter) {
    //         handler.LeadConverter(trigger.new);
    //     }       
    // }    
   //Map<id,Lead> lm = Trigger.newMap;
   //set<id> keys = lm.keySet();
   //SendSmsIntegration.sendMessage(keys);
}